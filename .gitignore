# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
# .env.production is intentionally committed for the operator console
# (apps/web) — it sets NEXT_PUBLIC_API_BASE_URL to the production
# server IP. See apps/web/.env.production for the full rationale.
!.env.production

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Test (added in PR 10)
coverage/
playwright-report/
test-results/
scripts/tests/test-keyfile-*

# Docker
pgdata/

# LLM tooling (cache, never tracked)
.atl/
legacy-test/

# Backups (never tracked)
*.bak

# CodeGraph index (regenerated locally, never tracked)
.codegraph/
