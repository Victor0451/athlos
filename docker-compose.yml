services:
  web:
    image: ${ATHLOS_WEB_IMAGE:?ATHLOS_WEB_IMAGE must be set to an immutable GHCR digest}
    restart: unless-stopped
    environment:
      API_INTERNAL_URL: http://api:3001
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/login"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s

  api:
    image: ${ATHLOS_API_IMAGE:?ATHLOS_API_IMAGE must be set to an immutable GHCR digest}
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "4000:3001"
    volumes:
      - ./backups:/var/backups/athlos
      - /tmp/legacy-placeholder:/legacy:ro
      # PR 8c.1 (athlos-socio-legajo): named volume for the
      # `socio_attachments` on-disk storage. Survives container
      # restarts; rolling deploy required so existing containers
      # pick up the mount.
      - storage:/app/storage
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  backups:
  storage:
