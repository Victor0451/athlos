# Web app environment variables (PR 8a.1).
# Copy to `.env.local` and adjust for your machine.

# API base URL — the deployed Fastify API.
# Dev: http://localhost:4001 (matches API v0.5.8 default).
# Prod: https://api.athlos.werchow.com.ar (filled in by the deploy slice).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4001