# Tasks: Club Dues Collection and Daily Cash

## Review Workload Forecast

| PR | Lines / review | Risk |
|---|---:|---|
| 1 Schema | 330 / 55m | High |
| 2 Calculator | 220 / 35m | Medium |
| 3 Repository | 360 / 60m | High |
| 3b Price mutations | ≤200 / ≤35m | Medium |
| 4 Service/audit | 340 / 55m | High |
| 5 Routes/flag | 280 / 45m | Medium |
| 5b Audit visibility | ≤200 / ≤35m | Medium |
| 6a Benefit rules | ≤300 / ≤50m | Medium |
| 6b Benefit application | ≤350 / ≤60m | High |
| 6c Family eligibility | ≤250 / ≤45m | Medium |
| 7–10 later slices | 250–390 / ≤60m each | High |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

Work units: 14 total; 14 completed, 0 pending. PR #309 is superseded/unmerged with no completion credit: it invented member-before-family/gross policy. PR6b is complete; PR6c was delivered under explicit maintainer authority and merged through PRs #317, #318, and #321, with CI hardening in #320. PR7/task 2.2 was merged as PR #323 at `051c77374241e28551d1fffd397d47d57fa5d7a0` under a maintainer-approved `size:exception`. PR8/task 2.3 was merged as PR #325 at `97e275fdf7bda4b7882c3439942457013992c079` on 2026-08-19 with commit `da08aa7 feat(dues): record agreements and work`; its final delivery diff is 1,038 additions / 8 deletions (1,046 changed lines), under a maintainer-approved `size:exception`. PR9/task 2.4 was approved through issue #326 (`status:approved`, `type:feature`, `size:exception`) and merged as PR #327 to `main` at `1a1c8f4c9e88c9e4e995ef4319dab383eb53280b` on 2026-08-19 with commit `e6d5078 feat(treasury): close audited cash shifts`; its authoritative committed diff is 2,503 additions / 118 deletions (2,621 changed lines). Task 2.5 is implemented locally only; parent owns delivery and no commit, push, issue, or PR was performed. The earlier pre-commit 2,623-line estimate is retained only as historical apply accounting below. Final scope includes migrations 0054–0057, the atomic cash/gasto lifecycle and force-close recovery, immutable included gastos/closes, privacy-safe audit, feature-gated resilient web UI, and optional one-way `ctacte` compatibility. The separate harness correction replaces shared `DROP SCHEMA` with a disposable per-file database. The inherited/current pre-existing untracked `.pi/gentle-ai/persona.json` remains excluded and untouched.

Delivery: ask-on-risk; stacked-to-main. PR 1→2→3→3b→4→5→5b→6a→6b→6c→…→10 merge sequentially to main; each PR starts from freshly updated main after its predecessor merges. Final PR7/task 2.2 delivery used a maintainer-approved `size:exception`; PR8 also used its approved exception; PR9 used issue #326's maintainer-approved `size:exception` and is merged as PR #327, with OpenSpec and `.pi` excluded from delivery paths.

## Phase 1: Native Pricing and Assessment Only

- [x] 1.1 **PR 1, RED/GREEN**: Test then add `packages/db/src/schema/dues.ts`, exports, `0049_dues_pricing_obligations.sql`. Dep: none; evidence: overlap/NK/immutability. Verify: `pnpm --filter @athlos/db test:run -- dues` (PostgreSQL fixture). Rollback: forward migration, preserve rows. Commit: `feat(dues): add immutable pricing ledger schema`.
- [x] 1.2 **PR 2, RED/GREEN**: Test then add `apps/api/src/modules/dues/calculator.ts#calculateAssessment` (cents, base/sports, rules). Dep: 1.1; evidence: two-sport/proration. Verify: `pnpm --filter @athlos/api test:run -- calculator` (N/A: pure). Rollback: module. Commit: `feat(dues): calculate versioned assessments`.
- [x] 1.3 **PR 3, RED/GREEN**: Test then add `apps/api/src/modules/dues/repository.ts#{claimReceipt,listEligibleMembers,insertObligation}`. Dep: 1.1–1.2; evidence: replay/concurrent-key one obligation. Verify: `ATHLOS_TEST_DATABASE_URL=… pnpm --filter @athlos/api test:run -- dues-repository` (race fixture). Rollback: writes. Commit: `feat(dues): persist idempotent assessments`.
- [x] 1.3b **PR 3b, RED/GREEN**: Test then add `apps/api/src/modules/dues/repository.ts#{createPrice,revokePrice}`. Dep: 1.3; evidence: effective-range overlap/conflict and complete revoke metadata. Verify: `ATHLOS_TEST_DATABASE_URL=… pnpm --filter @athlos/api test:run -- dues-repository` (PostgreSQL fixture). Rollback: price mutation ports only; no service/routes/schema/UI. Commit: `feat(dues): persist versioned price mutations`.
- [x] 1.4 **PR 4, RED/GREEN**: Test then add `apps/api/src/modules/dues/service.ts#{PricingService,AssessmentService}` and `packages/audit/src/emitter.ts#AuditAction`. Dep: 1.3b; evidence: snapshot, one atomic audit, no late fee. Verify: `pnpm --filter @athlos/api test:run -- dues-service` (transaction fixture). Rollback: service/actions. Commit: `feat(dues): audit assessment generation`.
- [x] 1.5 **PR 5, RED/GREEN**: Test then add `apps/api/src/routes/dues.ts`, `apps/api/src/server.ts`, `packages/config/src/schema.ts#DUES_ASSESSMENT_ENABLED`. Dep: 1.4; evidence: roles, gate, 400/403/409/503. Verify: `pnpm --filter @athlos/api test:run -- dues-routes` (Fastify inject). Rollback: flag/routes. Commit: `feat(dues): expose guarded assessment API`.
- [x] 1.5b **PR 5b, RED/GREEN**: Test then add privacy-safe authorized `metadata` DTO projection in `packages/audit/src/query.ts#queryAudit` and `apps/api/src/routes/audit.ts`. Dep: 1.5; evidence: authorized dues evidence appears; unauthorized metadata is redacted/shaped safely. Verify: `pnpm --filter @athlos/api test:run -- audit` (Fastify inject). Rollback: query/API DTO only; no dues mutation/schema/UI. Commit: `fix(audit): expose authorized dues evidence metadata`.

Boundary: no benefits, settlement, agreements/work, cash, expenses, UI, or projection. Native domain works with `ctacte` disabled; no first-tranche projection.

After PR5b merged, the scoped tranche check passed; do not mark final `sdd-verify`.

## Phase 2: Subsequent Capability Slices

- [x] 2.1a **PR 6a/13, RED/GREEN**: PostgreSQL tests then `packages/db/src/schema/dues-benefits.ts`, migration, and repository ports for priority, combinability/exclusive group, `GROSS|REMAINING`, member/family targets, authorization/effective/revocation metadata, overlap, and replacement. Dep: 1.5b. Verify: focused PostgreSQL tests. Rollback: benefit schema/repository only; no assessment/service/routes/audit. Commit: `feat(dues): add configurable benefit rules`.
- [x] 2.1b **PR 6b/13, RED/GREEN**: Tests then benefit application, assessment integration, routes/audits/privacy DTOs. Dep: 2.1a; evidence: configured priority, exclusive/combinable behavior, basis, floor zero, immutable BENEFIT snapshots, and optional `familyGroupId` evidence consumption. Verify: focused API/PostgreSQL tests. Rollback: benefit application only. Commit: `feat(dues): apply configured benefits`. Runtime family resolution is deferred to PR6c; this gated slice may merge first.

Benefit policy: every rule explicitly configures integer priority/order, combinability or exclusivity with an exclusive group/key as needed, and `GROSS` or `REMAINING` basis where percentage-based. No implicit member/family priority or gross-basis default exists; benefits never reduce an obligation below zero.

- [x] 2.1c **PR 6c, RED/GREEN**: PostgreSQL tests then effective-dated family-group/membership schema, migration, repository/admin commands, and eligibility lookup `familyGroupId`. Dep: merged 2.1b; evidence: one active group per socio/period, authorization/privacy/audit. Verify: focused PostgreSQL/API tests. Rollback: family eligibility only; no contacts, kinship, financial responsibility, or UI. Commit: `feat(dues): resolve effective family benefit groups`. Rerun scoped benefits check after merge.

Family decision: a socio has at most one active effective-dated benefit group per period; groups convey benefit eligibility only, never legal kinship, account ownership, or financial responsibility.

- [x] 2.2 **PR 7, RED/GREEN**: Test then add `settlements.ts#SettlementService`, `allocations.ts`, schema/routes. Dep: 2.1c; final delivery: PR #323 merged at `051c77374241e28551d1fffd397d47d57fa5d7a` with commit `fa7b12b feat(dues): allocate native settlements`; evidence: explicit allocations, non-cash evidence, idempotent replay, deterministic concurrency conflicts, immutable history, compensating reversals, restored reallocation, durable privacy-safe audit, guarded routes, and numeric bounds. Verify: PostgreSQL schema 14, settlement integration 6, focused unit/route/audit 20, and API/DB/audit quality checks all passed. Rollback: disable settlement gate/application surfaces; after migration `0052`, use a compensating forward migration preserving immutable history.
- [x] 2.3 **PR 8, RED/GREEN**: Test then add `agreements.ts#AgreementService`, `community-work.ts`, schemas/routes/audits. Dep: 2.2; final delivery: Issue #324 approved with `status:approved`, `type:feature`, and `size:exception`; PR #325 merged at `97e275fdf7bda4b7882c3439942457013992c079` on 2026-08-19 with commit `da08aa7 feat(dues): record agreements and work` and a 1,038-addition/8-deletion diff. Evidence: obligation-linked immutable agreements, approved bounded terms, active-only atomic supersession, canonical lock order/no deadlock, ACTIVE successor DB invariant, direct SQL bypass rejection, idempotency fingerprints, community-work non-cash allocations, unchanged cash, privacy-safe audit, and CI/quality checks passed. RDD was disabled; ordinary independent final validation observed no blocker, with no RDD receipt or approval claimed. Rollback: disable `DUES_AGREEMENTS_ENABLED`; after migration `0053`, use a forward compensating migration preserving debt/agreement history.
- [x] 2.4 **PR 9, RED/GREEN + ordinary corrective work**: Test then add `cash-desk.ts#CashDeskService`, close schema/routes, `gastos` guard, server/client feature gating, `apps/web/src/app/(authed)/tesoreria/*`, migrations `0054_dues_cash_closes.sql` through `0057_cash_lifecycle_boundaries.sql`, and the separate disposable per-file PostgreSQL harness correction. Issue #326 was approved with `status:approved`, `type:feature`, and `size:exception`; PR #327 merged to `main` at `1a1c8f4c9e88c9e4e995ef4319dab383eb53280b` on 2026-08-19 with commit `e6d5078 feat(treasury): close audited cash shifts` and a committed diff of 2,503 additions / 118 deletions (2,621 changed lines). RDD is disabled and no RDD receipt/approval is claimed. Final behavior is the atomic cash/gasto lifecycle, force-close recovery, immutable included gastos/closes, privacy-safe audit, feature-gated resilient web UI, and no `ctacte`: immediate included-OPEN-gasto immutability; database-atomic close-vs-update/delete/anular races; direct CLOSED insertion rejection; atomic compensation and audit writes; explicit idempotency/fingerprint replay/conflict; concurrent manual/open/close uniqueness; one-way DB lifecycle/source triggers; `America/Argentina/Jujuy` businessDate; inclusive movement interval; gasto-date equality; 24-hour movement block; authorized reasoned force-close recovery; privacy-safe compensation/recovery audit projection; cross-operator compensation keys; hidden disabled navigation; and handled web disabled/loading/error states. Final proof records focused cash PostgreSQL 5/5, default parallel API 115 files/862 tests, DB 23 files/128 tests, web/lifecycle/audit checks, and all quality checks passed. CI `check`, `test`, `drift-check`, Docker, `backup-bats`, `labeler`, and Playwright all passed. Final ordinary validation found no blocker; no RDD receipt or approval is claimed. No native acquire/settle or `ctacte` projection was performed. Rollback: disable the cash gate; for deployed migrations use forward compensating migrations preserving immutable close/compensation history.
- [x] 2.5 **PR 10, RED/GREEN + corrective rerun**: Test then add `ctacte-projection.ts#CtacteProjectionService`, `DUES_CTACTE_PROJECTION_ENABLED`, guarded projection route, allowlisted audit action, deterministic legacy mapping, bounded serialization retry, replay/fingerprint-conflict/skip/not-found visibility, positive `CREDITO`, reversal `DEBITO`, and PostgreSQL concurrency coverage. Dep: 2.2; evidence: disabled-native/single-retry projection with native ownership preserved and same-identity fingerprint conflict returning 409 without write/retry/success audit. Verify: focused API/PostgreSQL projection fixture plus native/legacy regression and final quality matrix recorded in apply-progress. Rollback: disable the compatibility gate; preserve native records and derived `ctacte` history. Commit intent: `feat(dues): add optional ctacte projection`.
