{
  "name": "@athlos/approval",
  "version": "0.6.0",
  "private": true,
  "description": "Athlos approval link service — opaque tokens, single-use + expiry, scoped to a target action.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/db": "workspace:*",
    "@athlos/errors": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
