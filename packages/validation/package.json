{
  "name": "@athlos/validation",
  "version": "0.5.17",
  "private": true,
  "description": "Athlos validation primitives — Zod schemas for ids, pagination, amounts, legacy keys, and per-resource contracts.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
