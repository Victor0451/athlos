{
  "name": "@athlos/auth",
  "version": "0.5.11",
  "private": true,
  "description": "Athlos auth primitives — bcrypt password hashing, JWT sign/verify, Fastify RBAC middleware.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/config": "workspace:*",
    "@athlos/errors": "workspace:*",
    "bcrypt": "^5.1.1",
    "fastify-plugin": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "@types/bcrypt": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.7",
    "fastify": "^5.2.0",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "fastify": "^5.2.0"
  }
}
