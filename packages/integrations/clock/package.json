{
  "name": "@athlos/integrations-clock",
  "version": "0.6.0",
  "private": true,
  "description": "Clock integration — real uses new Date(); stub lets tests advance time deterministically.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
