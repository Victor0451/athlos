{
  "name": "@athlos/import",
  "version": "0.5.18",
  "private": true,
  "description": "Athlos legacy import pipeline — DBF reader, hash, ordered import, bridge validation. PR 7a foundation.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/db": "workspace:*",
    "@athlos/errors": "workspace:*",
    "@athlos/integrations-legacy-db": "workspace:*",
    "dbf-reader": "^1.0.3",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
