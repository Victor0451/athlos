{
  "name": "@athlos/projection",
  "version": "0.4.0",
  "private": true,
  "description": "Athlos projection engine — rebuild domain projections + compute saldo. PR 7b.1a.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/db": "workspace:*",
    "@athlos/errors": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
