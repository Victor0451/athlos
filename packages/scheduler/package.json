{
  "name": "@athlos/scheduler",
  "version": "0.4.3",
  "private": true,
  "description": "Athlos in-process job scheduler — node-cron backed, DB-persisted job runs, retry + dead-letter, swap-in interface for future BullMQ migration.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/db": "workspace:*",
    "@athlos/errors": "workspace:*",
    "drizzle-orm": "^0.36.0",
    "node-cron": "^3.0.3",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.7.2"
  }
}
