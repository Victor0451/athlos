{
  "name": "@athlos/notifications",
  "version": "0.5.0",
  "private": true,
  "description": "Athlos cross-cutting notifications — dispatcher, channels (email, in-app, whatsapp), triggers (drift, import, approval).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./triggers": "./src/triggers/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/db": "workspace:*",
    "@athlos/errors": "workspace:*",
    "@athlos/integrations-email": "workspace:*",
    "@athlos/integrations-whatsapp": "workspace:*",
    "drizzle-orm": "^0.36.0",
    "nodemailer": "^6.9.16",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "@types/nodemailer": "^6.4.17",
    "typescript": "^5.7.2"
  }
}
