{
  "name": "@athlos/freshness",
  "version": "0.1.0",
  "private": true,
  "description": "Athlos freshness monitor — per-domain staleness status. PR 7b.1b.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@athlos/errors": "workspace:*"
  },
  "devDependencies": {
    "@athlos/vitest-config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
